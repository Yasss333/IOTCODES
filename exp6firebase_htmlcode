<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>🌡️ DHT11 Live Firebase Dashboard</title>
  <script type="module">
    // ===== Import Firebase Libraries =====
    import { initializeApp } from "https://www.gstatic.com/firebasejs/11.0.1/firebase-app.js";
    import { getDatabase, ref, onValue } from "https://www.gstatic.com/firebasejs/11.0.1/firebase-database.js";

    // ===== Your Firebase Configuration =====
    const firebaseConfig = {
      databaseURL: "https://expdw-27460-default-rtdb.firebaseio.com/"
    };

    // ===== Initialize Firebase =====
    const app = initializeApp(firebaseConfig);
    const db = getDatabase(app);

    // ===== Read Data from Firebase in Real-Time =====
    onValue(ref(db, "DHT11"), (snapshot) => {
      const data = snapshot.val();
      if (!data) return;
      document.getElementById("temp").innerText = data.temperature + " °C";
      document.getElementById("hum").innerText = data.humidity + " %";
      document.getElementById("time").innerText = new Date().toLocaleTimeString();
    });
  </script>

  <style>
    /* ===== Basic Styling ===== */
    body {
      font-family: 'Poppins', sans-serif;
      text-align: center;
      background: linear-gradient(135deg, #e3f2fd, #bbdefb);
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin: 0;
    }

    h1 {
      color: #1565c0;
      font-size: 2em;
      margin-bottom: 30px;
    }

    .card {
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      padding: 30px 60px;
      margin: 10px;
      transition: 0.3s;
    }

    .card:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(0,0,0,0.2);
    }

    .temp {
      color: #e53935;
      font-size: 2em;
    }

    .hum {
      color: #1e88e5;
      font-size: 2em;
    }

    #time {
      margin-top: 20px;
      color: #333;
      font-weight: bold;
    }

    footer {
      margin-top: 40px;
      font-size: 0.9em;
      color: #555;
    }
  </style>
</head>
<body>

  <h1>🌡️ DHT11 Firebase Live Dashboard</h1>

  <div class="card">
    <p>Temperature</p>
    <div id="temp" class="temp">-- °C</div>
  </div>

  <div class="card">
    <p>Humidity</p>
    <div id="hum" class="hum">-- %</div>
  </div>

  <p id="time">Waiting for data...</p>

  <footer>Built by  | ESP8266 + Firebase + DHT11</footer>

</body>
</html>
